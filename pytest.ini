[pytest]
addopts = -q --tb=short
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Ignore patterns
norecursedirs = .git .tox dist build *.egg node_modules

# Coverage (optional)
# addopts = -q --tb=short --cov=src --cov-report=term-missing
